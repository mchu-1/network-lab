{
  "name": "network-school-island",
  "version": "1.0.0",
  "description": "2D top-down game based on Network School island map",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.86.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.6",
    "vite-plugin-static-copy": "^3.1.4"
  }
}
